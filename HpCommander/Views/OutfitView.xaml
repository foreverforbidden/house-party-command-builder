<hp:TargetedCommandCategoryViewBase x:Class="HpCommander.Views.OutfitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hp="clr-namespace:HpCommander.Views"
             xmlns:controls="clr-namespace:HpCommander.Controls">
    <Border Style="{StaticResource CardStyle}">
        <StackPanel>
            <TextBlock Text="Outfit" Style="{StaticResource SectionHeaderStyle}" />

            <TextBlock Text="Outfit ID (list follows the selected character)" Style="{StaticResource FieldLabelStyle}" />
            <controls:FilteringComboBox x:Name="OutfitCombo" IsEditable="True" Width="320" HorizontalAlignment="Left" />

            <TextBlock Style="{StaticResource NoteTextStyle}" MaxWidth="360"
                       Text="Note: outfit has no false/undo form - to remove pieces afterward use the Change tab per-item." />
        </StackPanel>
    </Border>
</hp:TargetedCommandCategoryViewBase>
