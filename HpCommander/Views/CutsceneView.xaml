<hp:CommandCategoryViewBase x:Class="HpCommander.Views.CutsceneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hp="clr-namespace:HpCommander.Views"
             xmlns:controls="clr-namespace:HpCommander.Controls">
    <Border Style="{StaticResource CardStyle}">
        <StackPanel>
            <TextBlock Text="Cutscene" Style="{StaticResource SectionHeaderStyle}" />

            <!-- Shared across the tabs that use them. The scene was previously declared twice
                 (Play and End) and the character twice (star and random partner), so switching
                 tabs threw away the cast you had just chosen. -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <StackPanel x:Name="ScenePanel" Margin="0,0,16,0">
                    <TextBlock x:Name="SceneLabel" Text="Scene" Style="{StaticResource FieldLabelStyle}" />
                    <controls:FilteringComboBox x:Name="SceneCombo" Width="360" HorizontalAlignment="Left" />
                </StackPanel>
                <StackPanel x:Name="CharPanel">
                    <TextBlock x:Name="CharLabel" Text="Star" Style="{StaticResource FieldLabelStyle}" />
                    <controls:FilteringComboBox x:Name="CharCombo"
                                                Style="{StaticResource FilteringCharacterComboBoxStyle}"
                                                SelectionChanged="OnSelectionChanged" />
                </StackPanel>
            </StackPanel>

            <TabControl x:Name="ModeTabs" Style="{StaticResource SegmentedTabControlStyle}"
                        SelectionChanged="ModeTabs_SelectionChanged">

                <TabItem Header="Play" Style="{StaticResource SegmentedTabItemStyle}">
                    <StackPanel Margin="0,8,0,0">
                        <TextBlock Text="NPCs (add as many as the scene needs)" Style="{StaticResource FieldLabelStyle}" />
                        <StackPanel Orientation="Horizontal">
                            <ComboBox x:Name="Npc1" Width="150" Margin="0,0,8,0" SelectionChanged="OnSelectionChanged" />
                            <ComboBox x:Name="Npc2" Width="150" Margin="0,0,8,0" SelectionChanged="OnSelectionChanged" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <ComboBox x:Name="Npc3" Width="150" Margin="0,0,8,0" SelectionChanged="OnSelectionChanged" />
                            <ComboBox x:Name="Npc4" Width="150" SelectionChanged="OnSelectionChanged" />
                        </StackPanel>
                        <TextBlock Style="{StaticResource NoteTextStyle}" MaxWidth="440" Margin="0,10,0,0"
                                   Text="The cast is the star plus each NPC, in order. A scene needs its exact NPC count to play. Sex scenes require the Explicit Content DLC." />
                    </StackPanel>
                </TabItem>

                <TabItem Header="End" Style="{StaticResource SegmentedTabItemStyle}">
                    <StackPanel Margin="0,8,0,0">
                        <TextBlock Style="{StaticResource NoteTextStyle}" MaxWidth="440"
                                   Text="Ends the scene named above." />
                    </StackPanel>
                </TabItem>

                <TabItem Header="End Any" Style="{StaticResource SegmentedTabItemStyle}">
                    <StackPanel Margin="0,8,0,0">
                        <TextBlock Style="{StaticResource NoteTextStyle}" MaxWidth="440"
                                   Text="Ends whatever scene the Player is currently in." />
                        <TextBlock Text="cutscene EndAnySceneWithPlayer" FontFamily="Consolas" Margin="0,4,0,0" />
                    </StackPanel>
                </TabItem>

                <TabItem Header="Random" Style="{StaticResource SegmentedTabItemStyle}">
                    <StackPanel Margin="0,8,0,0">
                        <TextBlock Text="Zone" Style="{StaticResource FieldLabelStyle}" />
                        <controls:FilteringComboBox x:Name="RandomZoneCombo" Width="280" HorizontalAlignment="Left" />
                        <TextBlock Style="{StaticResource NoteTextStyle}" MaxWidth="440" Margin="0,10,0,0"
                                   Text="Plays a random scene from the given zone between the Player and the character chosen above." />
                    </StackPanel>
                </TabItem>
            </TabControl>
        </StackPanel>
    </Border>
</hp:CommandCategoryViewBase>
