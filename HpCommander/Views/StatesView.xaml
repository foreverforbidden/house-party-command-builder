<hp:TargetedCommandCategoryViewBase x:Class="HpCommander.Views.StatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hp="clr-namespace:HpCommander.Views"
             xmlns:controls="clr-namespace:HpCommander.Controls">
    <Border Style="{StaticResource CardStyle}">
        <StackPanel>
            <TextBlock Text="States" Style="{StaticResource SectionHeaderStyle}" />
            <TextBlock Text="State (tip: check 'All Characters' as target for all.states(...))"
                       Style="{StaticResource FieldLabelStyle}" MaxWidth="360" TextWrapping="Wrap" />
            <controls:FilteringComboBox x:Name="StateCombo" IsEditable="True" Width="300" HorizontalAlignment="Left" />
        </StackPanel>
    </Border>
</hp:TargetedCommandCategoryViewBase>
